{
  "name": "carbide-contracts",
  "version": "1.0.0",
  "description": "CarbideEscrow smart contracts with Hardhat and OpenZeppelin",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "setup-verifier": "hardhat run scripts/setup-verifier.ts --network arbitrumSepolia",
    "fund-faucet": "hardhat run scripts/fund-faucet.ts --network arbitrumSepolia",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}
